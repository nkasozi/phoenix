[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "example_lib_1"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "scikit-learn",
]
